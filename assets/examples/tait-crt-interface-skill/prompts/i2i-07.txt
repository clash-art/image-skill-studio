Use case: style-transfer
Asset type: strict example output for tait-crt-interface-skill
Input images: Image 1 is the sole source object reference for semantic anchors only, never a tracing, cutout, contour map, photo filter, auto-pixelation target, or prior result.

Create one finished landscape 16:9 CRT retro computer-interface illustration. No named preset. Exactly one object subject S1: manual espresso machine anchored by tall rectangular steel body, oversized lever rising to the right, round pressure gauge at upper left, turquoise-like drip-tray plane, two opposing dark wooden handle cues, and tiny red indicator. Delete reflections, brushed-metal texture, exact hardware, screw detail, photographic contours, source perspective and shadow. Rebuild independently as an adult industrial bitmap editorial machine-caricature: body becomes a broad skewed monolith, lever doubles in size as a diagonal mast, gauge becomes an offset square-ring eye, tray projects as a huge stepped tongue, handles become blunt opposing wedges. Use 6-8 interlocking flat masses, curve-free diagonal-facet geometry, approximately 30x30 contour budget on the global lattice. One object-spread diagonal-right wallpaper composition covers 80%; no duplicate machine.

Use exactly 3 foreground windows in underlay-cross constellation, hierarchy 1 large terminal + 1 medium + 1 small, distributed across three quadrants with 5-20% overlap crossing the machine and preserving 20% connected open field. Exactly 1 small square partial extraction showing only the gauge face and indicator relationship, not the whole machine. Include full-width early Macintosh/Minitel menu, one open French dropdown, squared title bars/close boxes/scroll tracks, terminal rows, primitive pressure readout, short French bitmap labels, exactly one cursor. Upper-right title-bar zone renders exact readable lowercase "tait-crt-interface-skill", unobstructed.

Use only palette 复古01: #efca54, #5d9f58, #e870a1, #bbb8a5, #49473c. Split-local-fields polarity, broad flat planes. Allow one compact regular #bbb8a5/#49473c checkerboard only on the non-organic gauge ring if needed; no decorative checks. Object, UI, glyphs, icons, cursor, charts and signal marks use one shared complete integer-aligned square pixel lattice at identical scale. No gradient, alpha, antialiasing, computed gray, smooth curves, mixed cells, realistic rendering, 3D, game HUD, modern cards, brand/logo, CTA or watermark.

The single generated image is the final artifact. Render hard square bitmap cells and glyphs, dense palette-bound scanlines, hard-cell bloom, sparse noise, short persistence, one-cell misregistration and one restrained sync-band fault. Force unmistakable spherical barrel curvature/compression in outer 10% of all four edges, stronger at corners with visibly bowed top menu and side rails, central 80% stable. No monitor bezel or room. Exact signature stays crisp.
